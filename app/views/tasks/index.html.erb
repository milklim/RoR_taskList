<div>
  <%= link_to 'New task', 'tasks/new' %>
  <% @tasks.each do |task| %>
    <div>
      <%= task.name %>
      <%= link_to 'Updade', edit_task_path(id: task.id) %>
      <%= link_to 'Delete', del_task_path(id: task.id), method: :delete %>
    </div>
  <% end %>
  
</div>